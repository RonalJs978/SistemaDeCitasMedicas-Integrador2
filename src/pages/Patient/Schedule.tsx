import { useState, useEffect } from 'react'
import { useAppointments } from '../../context/AppointmentContext'

export default function PatientSchedule() {
  return <div>Agendar cita - Paciente</div>
}